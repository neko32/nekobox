# cargo audit 設定ファイル
# https://docs.rs/cargo-audit/latest/cargo_audit/config/

[advisories]
ignore = [
    # RUSTSEC-2023-0071: rsa クレートの Marvin Attack (timing sidechannel)
    # 影響範囲: sqlx-mysql 経由の間接依存。
    # nekobox-backend は SQLite のみ使用しており MySQL は無効化されているため
    # このクレートは実際にはリンクされず、実行時リスクはない。
    # 修正版リリース待ち (sqlx 側で未対応)。
    "RUSTSEC-2023-0071",
]
